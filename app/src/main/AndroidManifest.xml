<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gal.caronte">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- GMaps api key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="gal.caronte.activity.InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="gal.caronte.activity.MapaActivity">
        </activity>

        <activity
            android:name="gal.caronte.activity.DetallePoiActivity">
        </activity>

        <activity
            android:name="gal.caronte.activity.DetallePercorridoActivity">
        </activity>

        <activity
            android:name="gal.caronte.activity.DatosImaxeActivity">
        </activity>

        <activity
            android:name="gal.caronte.activity.ImaxeActivity">
        </activity>

    </application>

</manifest>